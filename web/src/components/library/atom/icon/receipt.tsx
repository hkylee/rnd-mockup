// AUTO-GENERATED — icon (icon-registry 기반)
// name: atom/icon/receipt / svg: (skeleton)
import * as React from "react";

interface ReceiptProps {
  style?: React.CSSProperties;
}

export function Receipt(props: ReceiptProps = {} as ReceiptProps) {
  return (
    <div
      data-component="atom/icon/receipt"
      style={{
        width: 24,
        height: 24,
        background: "var(--semantic-color-icon-container-default, #ecedef)",
        borderRadius: 4,
        display: "inline-block",
        flexShrink: 0,
        ...props.style,
      }}
    />
  );
}

export default Receipt;
