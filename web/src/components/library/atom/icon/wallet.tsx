// AUTO-GENERATED — icon (icon-registry 기반)
// name: atom/icon/wallet / svg: (skeleton)
import * as React from "react";

interface WalletProps {
  style?: React.CSSProperties;
}

export function Wallet(props: WalletProps = {} as WalletProps) {
  return (
    <div
      data-component="atom/icon/wallet"
      style={{
        width: 24,
        height: 24,
        background: "var(--semantic-color-icon-container-default, #ecedef)",
        borderRadius: 4,
        display: "inline-block",
        flexShrink: 0,
        ...props.style,
      }}
    />
  );
}

export default Wallet;
