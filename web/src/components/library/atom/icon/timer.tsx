// AUTO-GENERATED — icon (icon-registry 기반)
// name: atom/icon/timer / svg: (skeleton)
import * as React from "react";

interface TimerProps {
  style?: React.CSSProperties;
}

export function Timer(props: TimerProps = {} as TimerProps) {
  return (
    <div
      data-component="atom/icon/timer"
      style={{
        width: 24,
        height: 24,
        background: "var(--semantic-color-icon-container-default, #ecedef)",
        borderRadius: 4,
        display: "inline-block",
        flexShrink: 0,
        ...props.style,
      }}
    />
  );
}

export default Timer;
