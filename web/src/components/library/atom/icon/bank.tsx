// AUTO-GENERATED — icon (icon-registry 기반)
// name: atom/icon/bank / svg: (skeleton)
import * as React from "react";

interface BankProps {
  style?: React.CSSProperties;
}

export function Bank(props: BankProps = {} as BankProps) {
  return (
    <div
      data-component="atom/icon/bank"
      style={{
        width: 24,
        height: 24,
        background: "var(--semantic-color-icon-container-default, #ecedef)",
        borderRadius: 4,
        display: "inline-block",
        flexShrink: 0,
        ...props.style,
      }}
    />
  );
}

export default Bank;
