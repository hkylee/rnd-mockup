// AUTO-GENERATED — icon (icon-registry 기반)
// name: atom/icon/card / svg: (skeleton)
import * as React from "react";

interface CardProps {
  style?: React.CSSProperties;
}

export function Card(props: CardProps = {} as CardProps) {
  return (
    <div
      data-component="atom/icon/card"
      style={{
        width: 24,
        height: 24,
        background: "var(--semantic-color-icon-container-default, #ecedef)",
        borderRadius: 4,
        display: "inline-block",
        flexShrink: 0,
        ...props.style,
      }}
    />
  );
}

export default Card;
